# Safe Device Changes

On real systems, get confirmation before persistent changes.

Default safe approach:

- inspect first
- propose exact commands or UI actions
- execute only after confirmation if the target is real or productive
