# Security Model

ctrlX OS combines user roles, app confinement, device services, and network-facing APIs.

Practical rules:

- use least privilege when suggesting customer solutions
- do not treat every denial log as a bug
- separate documented defaults from customer-specific credentials
- avoid embedding tokens or passwords in examples when a safer local mechanism exists
