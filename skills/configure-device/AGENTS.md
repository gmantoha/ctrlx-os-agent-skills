# Configure Device Workflow

Use this workflow for persistent system configuration tasks.

Rules:

- inspect current state first
- prefer the least invasive interface that fits the task
- on real devices, confirm before writing changes
- document exact UI, REST, SSH, or WebDAV steps used
