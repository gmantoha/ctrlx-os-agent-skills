# Web UI Access

Use the Web UI as the default interaction path for guided configuration, visual checks, and workflows that are easier to validate interactively than over shell or REST.

Where public defaults apply, credentials are typically `boschrexroth` / `boschrexroth`.

Use Playwright when the steps should be repeatable or demonstrated.

On real devices, confirm before making persistent configuration changes through the Web UI.
