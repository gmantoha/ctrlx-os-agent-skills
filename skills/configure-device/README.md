# Configure Device

Workflow for network, firewall, storage, user, and related ctrlX OS configuration tasks.

Prefer Web UI or REST for persistent configuration changes. Use SSH mainly for inspection, logs, and diagnostics unless a documented workflow requires shell-level changes.
